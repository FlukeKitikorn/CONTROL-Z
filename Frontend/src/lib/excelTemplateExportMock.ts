/**
 * REFACTOR(CANDIDATE-REMOVAL): ไม่ถูก import หลังถอดปุ่ม export Excel — Phase A dead-code audit
 * โค้ดเดิมอยู่ใน git history (block comment ไม่ได้เพราะมี JSDoc ปิด comment ภายในไฟล์)
 */
export {}
